export interface User {
    id: string | number,
    userId?: string | number
    user?: {}
    userInfos?: {},
    activity?: {},
    average?: {},
    performance?: {},
    score?: string | number,
    todayScore?: string | number
}